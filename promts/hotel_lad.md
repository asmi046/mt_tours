Сделай чтобы комманда app/Console/Commands/AleanGetHotels.php работала по следующему алгоритму:
- проверяла таблицу alean_tours если она пуста прерывала процесс выполнения
- Выполняется запрос к таблице alean_tours в котором выбираются "hotelId", "hotelCID" и происходит групировка по "hotelId"
- результат выводится на экран


В переменной $alean_data['HotelImageList'] метода getHotelStructuresDescription хранятся элеменеты вот такого вида:
{
            "Url": "https://uploads2.stells.info/storage/png/6/a8/6a86df9773f67a89dd8523ba042191b9.png",
            "Index": "0",
            "Description": "«Феодосия» отель"
        },

Нужно сделать следующее:
- скачать файл по представленному url, преобразовать его в jpg со сжатием 7, поместить сжатый файл в хранилище в папку alean-sea-hotels
- сделать массив вида [
    'img'=> <адрес сжатого файла в хранилище>, 
    'title'=> <"Description" из исходного массива>, 
]

- преобразовать массив в json и присвоить переменной $structures['gallery']

- Первую фотографию из списка нужно присвоить переменной $structures['img']

Вынеси данный функционал в отдельный метод и вызови его в методе etHotelStructuresDescription
